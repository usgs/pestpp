name: pestpp_tester
channels:
  - conda-forge
  - nodefaults
dependencies:
  # required
  - python<=3.11
  - numpy>=1.15.0
  - pandas
  - scipy
  # optional
  - matplotlib>=1.4.0
  - pyshp
  - jinja2
  # tests
  - coveralls
  - nose-timer
  - pytest-xdist
  - nbmake
  # imported directly by benchmarks/pestpp_api_tests.py to execute the example
  # notebook; nbmake pulls them in too, but depending on that transitively is fragile
  - nbclient
  - nbformat
  - shapely
  - flopy
  - mfpymake
  - nose
  - pip
  - git
  - scikit-learn
  - pip:
    - git+https://github.com/pypest/pyemu.git@develop
    - git+https://github.com/modflowpy/flopy.git
    - laGpy